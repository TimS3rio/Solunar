object Testing {
    private const val junitVersion = "4.13.2"
    const val junit = "junit:junit:$junitVersion"

    private const val extJunitVersion = "1.1.5"
    const val extJunit = "androidx.test.ext:junit:$extJunitVersion"

    private const val espressoCoreVersion = "3.5.1"
    const val espressoCore = "androidx.test.espresso:espresso-core:$espressoCoreVersion"

    const val composeJunit = "androidx.compose.ui:ui-test-junit4"
    const val composeTooling = "androidx.compose.ui:ui-tooling"
    const val composeManifest = "androidx.compose.ui:ui-test-manifest"

    private const val mockWebServerVersion = "5.0.0-alpha.11"
    const val mockWebServer = "com.squareup.okhttp3:mockwebserver:$mockWebServerVersion"

    private const val testRuleVersion = "1.5.0"
    const val testRule = "androidx.test:rules:$testRuleVersion"

    private const val composeNavigationVersion = "2.6.0"
    const val composeNavigation = "androidx.navigation:navigation-testing:$composeNavigationVersion"

    private const val uiAutomatorVersion = "2.3.0-alpha03"
    const val uiAutomator = "androidx.test.uiautomator:uiautomator:$uiAutomatorVersion"

    private const val coroutinesVersion = "1.6.0"
    const val coroutines = "org.jetbrains.kotlinx:kotlinx-coroutines-test:$coroutinesVersion"

    private const val orchestratorVersion = "1.4.2"
    const val orchestrator = "androidx.test:orchestrator:$orchestratorVersion"

    private const val mockitoVersion = "5.2.1"
    const val mockito = "org.mockito.kotlin:mockito-kotlin:$mockitoVersion"
}
